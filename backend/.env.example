# ML
ML_ENABLED=true
ML_MODEL_PATH=ml/saved_models/lstm_model.pt

# CORS
CORS_ORIGINS=["*"]

# Rate Limiting
RATE_LIMIT_PER_MINUTE=100

# Cache TTL (seconds)
CACHE_TTL_CRYPTO=3
CACHE_TTL_OTHER=10

# Server
HOST=0.0.0.0
PORT=8000
